# ── Local server ─────────────────────────────────────────────────────────────
PORT=4173
HOST=0.0.0.0

# ── AllAnime (primary online source — no key required) ───────────────────────
# No configuration needed; the proxy at /api/allanime/* handles it automatically.

# ── JIMOV / TioAnime (primary online source — no key required) ───────────────
# Defaults to the public JIMOV instance. Override only if you self-host.
JIMOV_API=https://jimov-api.vercel.app

# ── ZenkaiTV scraper service (TioAnime + AnimeAV1 sources) ───────────────────
# The Python Flask service (folder: scraper / your test repo, app.py).
# Local:  http://localhost:5000
# Hosted: set to your deployed scraper URL, e.g. https://zenkaitv-scraper.onrender.com
TIOANIME_API=http://localhost:5000

# ── Anime1v (optional local/self-hosted source) ───────────────────────────────
# Local default. For Vercel/online deployment replace with a public HTTPS URL,
# or leave unset — AnimeTV will gracefully skip it when unreachable.
ANIME1V_API=http://localhost:3001
ANIME1V_PATH=
ANIME1V_API_KEY=dev-anime1v-key
ANIME1V_AUTO_START=true

# ── Consumet KickAssAnime (optional self-hosted source) ──────────────────────
# Local default. For Vercel/online deployment use a public HTTPS Consumet instance
# you control (e.g. deploy riimuru/consumet-api on Railway or Render).
# Leave unset to skip this source gracefully.
CONSUMET_API=http://localhost:3000
CONSUMET_CATALOG_LIMIT=360
CONSUMET_SEARCH_PAGES=6
CONSUMET_TIMEOUT_MS=7000

# ── RapidAPI Anime (optional paid source) ────────────────────────────────────
# Set both variables if you have a RapidAPI subscription.
# Leave blank to skip this source gracefully.
RAPIDAPI_ANIME_HOST=your-host.p.rapidapi.com
RAPIDAPI_ANIME_KEY=your-rapidapi-key-here
RAPIDAPI_ANIME_TIMEOUT_MS=18000
RAPIDAPI_ANIME_CATALOG_LIMIT=300

# ── TMDB (episode stills, season posters, cinematic backdrops) ───────────────
# AniList provides covers/banners but NOT episode thumbnails; TMDB fills that
# gap. Get a free key at https://www.themoviedb.org/settings/api and set EITHER:
#   • TMDB_API_KEY              — the v3 "API Key"
#   • TMDB_READ_ACCESS_TOKEN    — the v4 "API Read Access Token" (Bearer)
# Leave both blank to skip TMDB gracefully — the app falls back to AniList art.
# On Vercel, add the variable under Project → Settings → Environment Variables.
TMDB_API_KEY=
TMDB_READ_ACCESS_TOKEN=
# Optional public server-side proxy used by local/Android when no key is
# installed on that device. The TMDB credential remains private on Vercel.
TMDB_PROXY_BASE=https://zxkai.fun/api/tmdb
TMDB_TIMEOUT_MS=12000

# ── Misc ─────────────────────────────────────────────────────────────────────
UPDATE_REPO_URL=https://github.com/JSolanoDev/AnimeTV
DAILY_REFRESH_INTERVAL_MS=86400000
LOG_LEVEL=info

# ── Supabase Authentication (required for login: Google / Facebook / email) ───
# Create a free project at https://supabase.com → Project Settings → API.
# The app serves these to the browser via GET /api/config, so use the PUBLIC
# "anon" / "publishable" key — NEVER the service_role key (it must stay secret).
#
# The server accepts the URL and the public key under ANY of these names, so set
# whichever your tooling uses (plain / Next.js / Vite / SvelteKit). The Vercel
# Supabase integration creates SUPABASE_URL + SUPABASE_ANON_KEY automatically.
#   URL: SUPABASE_URL | NEXT_PUBLIC_SUPABASE_URL | VITE_SUPABASE_URL | PUBLIC_SUPABASE_URL
#   key: SUPABASE_KEY | SUPABASE_ANON_KEY | NEXT_PUBLIC_SUPABASE_ANON_KEY | VITE_SUPABASE_ANON_KEY | PUBLIC_SUPABASE_ANON_KEY
#
# Local: put them in .env / .env.local. On Vercel: Project → Settings →
# Environment Variables (scope = Production), then REDEPLOY for them to apply.
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_KEY=your-anon-public-key

# Vite-style aliases (the server reads these too — set instead of, not in
# addition to, the names above):
# VITE_SUPABASE_URL=https://your-project.supabase.co
# VITE_SUPABASE_ANON_KEY=your-anon-public-key

