name: Scrape anime catalog

# ── Triggers ────────────────────────────────────────────────────────────────
on:
  schedule:
    # Once per day at 06:00 UTC
    - cron: '0 6 * * *'

  workflow_dispatch:
    inputs:
      scrapeEpisodes:
        description: 'Fetch episode URLs for top shows'
        required: false
        default: 'true'
        type: choice
        options: ['true', 'false']
      top:
        description: 'Top N shows to enrich with episodes'
        required: false
        default: '20'
      maxEpisodes:
        description: 'Max episodes per show (most-recent first)'
        required: false
        default: '5'
      catalogPages:
        description: 'AnimeAV1 catalog pages (0 = every available page)'
        required: false
        default: '0'
      jikanEnrich:
        description: 'Enrich metadata via Jikan/MAL (synopsis, score, etc.)'
        required: false
        default: 'false'
        type: choice
        options: ['false', 'true']

# Opt into Node 24 runtime for all referenced actions
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: 'true'

permissions:
  contents: write

concurrency:
  group: scrape-catalog
  cancel-in-progress: false

# ── Jobs ─────────────────────────────────────────────────────────────────────
jobs:
  scrape:
    name: Scrape + validate + commit catalog
    runs-on: ubuntu-latest
    # The individually bounded scrape, artwork, season, skip-time, and adult
    # refresh steps can legitimately exceed 90 minutes when an upstream is
    # slow. Keep the job bounded, but leave enough room for every validation
    # gate to run instead of cancelling a healthy daily refresh near the end.
    timeout-minutes: 240

    steps:
      # ── 1. Checkout ────────────────────────────────────────────────────────
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          fetch-depth: 50

      # ── 2. Python setup ────────────────────────────────────────────────────
      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip
          cache-dependency-path: scraper/requirements.txt

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      # ── 3. Install dependencies ────────────────────────────────────────────
      - name: Install Python dependencies
        run: pip install -r scraper/requirements.txt

      - name: Test AnimeAV1 catalog discovery
        run: python -m unittest discover -s scraper -p 'test_*.py'

      # ── 4. Build scraper arguments ─────────────────────────────────────────
      - name: Build scraper arguments
        id: args
        run: |
          # Scheduled run defaults
          # Regular anime is refreshed from AnimeAV1 only.
          SITES="animeav1"
          SCRAPE_EPS="true"
          TOP="20"
          MAX_EPS="5"
          CATALOG_PAGES="0"
          JIKAN_ENRICH="false"

          # Override with workflow_dispatch inputs when present
          if [ "${{ github.event_name }}" = "workflow_dispatch" ]; then
            SCRAPE_EPS="${{ github.event.inputs.scrapeEpisodes }}"
            TOP="${{ github.event.inputs.top }}"
            MAX_EPS="${{ github.event.inputs.maxEpisodes }}"
            CATALOG_PAGES="${{ github.event.inputs.catalogPages }}"
            JIKAN_ENRICH="${{ github.event.inputs.jikanEnrich }}"
          fi

          # Build argument string
          EXTRA_ARGS="--sites ${SITES} --top ${TOP} --max-eps ${MAX_EPS} --catalog-pages ${CATALOG_PAGES} --no-jikan-fallback"

          if [ "$SCRAPE_EPS" = "true" ]; then
            EXTRA_ARGS="--episodes ${EXTRA_ARGS}"
          fi

          if [ "$JIKAN_ENRICH" = "true" ]; then
            EXTRA_ARGS="${EXTRA_ARGS} --jikan-enrich"
          fi

          echo "extra_args=${EXTRA_ARGS}" >> "$GITHUB_OUTPUT"
          echo "Scraper args: ${EXTRA_ARGS}"

      # ── 5. Run scraper ─────────────────────────────────────────────────────
      # Exits 0 on success or when previous catalog is kept.
      # Exits 1 only when 0 items AND no previous catalog exists.
      - name: Run anime scraper
        id: scrape
        run: python scraper/anime_scraper.py ${{ steps.args.outputs.extra_args }}
        timeout-minutes: 32

      # ── 6. Validate output ─────────────────────────────────────────────────
      - name: Validate catalog JSON
        run: |
          python - <<'PYEOF'
          import json, sys
          path = "scraper/anime_metadata.json"
          try:
              with open(path, encoding="utf-8") as f:
                  data = json.load(f)
          except FileNotFoundError:
              print("SKIP: anime_metadata.json not written (scraper kept previous). OK.")
              sys.exit(0)
          items    = data.get("items") or []
          ep_count = sum(len(i.get("episodes") or []) for i in items)
          sources  = data.get("sources") or [data.get("source", "?")]
          print(f"  Anime count    : {len(items)}")
          print(f"  Episode URLs   : {ep_count}")
          print(f"  Sources        : {sources}")
          print(f"  Scraped at     : {data.get('scrapedAt', '?')}")
          if len(items) == 0:
              # 0 items means the scraper kept the existing file unchanged.
              # The catalog commit step will detect no diff and skip the commit.
              # Do NOT fail the workflow — this is expected when all sites are unreachable.
              print("NOTE: catalog has 0 items — file unchanged, no commit will be made.")
              sys.exit(0)
          print("Validation OK — ready to commit.")
          PYEOF

      - name: Refresh exact playable episode inventory
        run: npm run catalog:inventory
        timeout-minutes: 18

      # New catalogue rows do not carry MAL/AniList ids. Resolve them against a
      # versioned offline database first, then let the resumable artwork builder
      # add season-specific TMDB imagery. Every step preserves yesterday's map
      # when an upstream is unavailable; the integrity gate below checks the
      # resulting coverage before anything can be committed.
      - name: Download offline anime identity database
        continue-on-error: true
        run: |
          node <<'NODE'
          const fs = require('node:fs');
          const path = require('node:path');
          (async () => {
            const headers = { Accept: 'application/vnd.github+json', 'User-Agent': 'ZenkaiTV-catalog-workflow' };
            const release = await fetch('https://api.github.com/repos/manami-project/anime-offline-database/releases/latest', { headers });
            if (!release.ok) throw new Error(`release metadata HTTP ${release.status}`);
            const body = await release.json();
            const asset = (body.assets || []).find((entry) => entry.name === 'anime-offline-database.jsonl');
            if (!asset) throw new Error('offline database asset is missing');
            const download = await fetch(asset.browser_download_url, { headers });
            if (!download.ok) throw new Error(`offline database HTTP ${download.status}`);
            const output = path.join(process.env.RUNNER_TEMP, 'anime-offline-database.jsonl');
            fs.writeFileSync(output, Buffer.from(await download.arrayBuffer()));
            console.log(`wrote ${output}`);
          })().catch((error) => { console.error(error.message); process.exit(1); });
          NODE

      - name: Resolve new regular catalog identities
        continue-on-error: true
        run: |
          if [ -f "$RUNNER_TEMP/anime-offline-database.jsonl" ]; then
            node scripts/resolve-from-offline-db.mjs --db "$RUNNER_TEMP/anime-offline-database.jsonl" --write
            node scripts/fill-meta-from-offline-db.mjs --db "$RUNNER_TEMP/anime-offline-database.jsonl" --write
          else
            echo "Offline identity database unavailable; keeping the previous identity map."
          fi

      - name: Refresh high-quality regular artwork
        continue-on-error: true
        run: node scripts/build-artwork-map.mjs --base https://zxkai.fun --concurrency 2 --max-minutes 15
        timeout-minutes: 18

      - name: Refresh regular title metadata
        continue-on-error: true
        run: node scripts/add-artwork-metadata.mjs --max-minutes 9
        timeout-minutes: 12

      # graphql.anilist.co answers 403 to Vercel, to the browser AND to this
      # runner - run #111 finished this step in 3 seconds, where ~40 batched
      # requests at a 1200ms pause cannot finish inside a minute. The idea that
      # Actions is "a different network" was simply wrong.
      #
      # So the chains come from Jikan, which labels its relations Prequel/Sequel
      # exactly as AniList did, plus the manami offline database for each entry's
      # title, episode count and AniList id. ~600 catalogue rows sit beside
      # another TV entry, and following each chain outward to the seasons we do
      # not carry adds more, capped at 1400 requests paced at 1.1s - roughly 26
      # minutes. The script still cannot fail this job: it exits 0 whatever
      # happens and always leaves a valid map behind.
      - name: Bake airing schedules and season chains
        run: node scripts/build-airing-map.mjs --write --max-minutes 32 --db "$RUNNER_TEMP/anime-offline-database.jsonl"
        timeout-minutes: 40
        continue-on-error: true

      - name: Seed related season artwork identities
        continue-on-error: true
        run: |
          if [ -f "$RUNNER_TEMP/anime-offline-database.jsonl" ]; then
            node scripts/seed-anilist-rows.mjs --db "$RUNNER_TEMP/anime-offline-database.jsonl" --airing scraper/airing-map.json --write
            node scripts/fill-meta-from-offline-db.mjs --db "$RUNNER_TEMP/anime-offline-database.jsonl" --write
          else
            echo "Offline identity database unavailable; keeping existing related-season artwork."
          fi

      - name: Refresh high-quality related season artwork
        continue-on-error: true
        run: node scripts/build-artwork-map.mjs --base https://zxkai.fun --concurrency 2 --max-minutes 15
        timeout-minutes: 18

      - name: Sync regular catalog assets to Android
        run: |
          cp scraper/artwork-map.json android/app/src/main/assets/scraper/artwork-map.json
          cp scraper/regular-source-fallbacks.json android/app/src/main/assets/scraper/regular-source-fallbacks.json

      - name: Refresh opening and ending skip times
        run: >-
          node scripts/build-aniskip-map.mjs
          --catalog scraper/anime_metadata.json
          --max-episodes 2000
          --max-lookups 1200
          --concurrency 4
          --recheck-days 7
        timeout-minutes: 18
        continue-on-error: true

      - name: Refresh Adult Mode catalog
        run: npm run adult:catalog
        env:
          UNDERHENTAI_CRAWL_CONCURRENCY: '2'
          UNDERHENTAI_DETAIL_CONCURRENCY: '2'
          UNDERHENTAI_WATCH_CONCURRENCY: '2'
          UNDERHENTAI_REQUEST_INTERVAL_MS: '550'
        timeout-minutes: 45

      - name: Validate Adult Mode catalog
        run: npm run adult:verify && npm run test:releases

      - name: Validate regular catalog, playback identity, and seasons
        run: |
          npm run catalog:verify -- --restore-on-failure --report artifacts/catalog-integrity-report.json
          npm run test:catalog
          npm run test:pipeline
          npm run test:seasons
          node scripts/test-airing-map.mjs .
          node scripts/test-season-stills.mjs .
          node scripts/test-image-delivery.mjs

      - name: Refresh homepage starter catalog
        run: node scripts/build-homepage-bootstrap.mjs

      # ── 7. Commit updated catalog ──────────────────────────────────────────
      - name: Commit updated catalog
        id: commit-catalog
        shell: bash
        run: bash scripts/commit-catalog-update.sh

      - name: Trigger Vercel deploy hook
        if: steps.commit-catalog.outputs.changes_detected == 'true'
        env:
          VERCEL_DEPLOY_HOOK_URL: ${{ secrets.VERCEL_DEPLOY_HOOK_URL }}
        run: |
          if [ -z "$VERCEL_DEPLOY_HOOK_URL" ]; then
            echo "VERCEL_DEPLOY_HOOK_URL is not configured; relying on the Vercel Git integration."
            exit 0
          fi
          curl -fsS -X POST "$VERCEL_DEPLOY_HOOK_URL"

      # ── 8. Upload logs on failure ──────────────────────────────────────────
      - name: Upload scraper artifacts on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: scraper-failure-${{ github.run_id }}
          path: |
            scraper/anime_metadata.json
            scraper/anime_metadata.previous.json
            scraper/anime_metadata.csv
            scraper/anime_metadata.rejected.json
            scraper/artwork-map.json
            scraper/airing-map.json
            scraper/aniskip-map.json
            scraper/relations-cache.json
            scraper/regular-source-fallbacks.json
            scraper/underhentai_catalog.json
            scraper/underhentai_details.json
            artifacts/catalog-integrity-report.json
          retention-days: 7
          if-no-files-found: warn
